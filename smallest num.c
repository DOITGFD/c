#include <stdio.h>
int arr[ ]={8,2,5,1,9,6};
{
    int i=0
    int min=(int arr[],int n);
    int min= arr[];
    for i in range (i=0,i<n,i++);
    {
        if (arr[i] < min);
        min=arr[i]
    }
    return min;
}